import 'package:flutter/material.dart';

class SafeInfosPage extends StatelessWidget {
  const SafeInfosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
